import CryptoJS from"crypto-js";import{CUSTOM_KEY}from"../FirebaseEnv/firebaseEnv.js";const decrypt=t=>{try{const r=CryptoJS.AES.decrypt(t,CUSTOM_KEY);return r.toString(CryptoJS.enc.Utf8)}catch(t){}};export default decrypt;